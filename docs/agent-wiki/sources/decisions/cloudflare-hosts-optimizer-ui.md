# UI placement

QNAP Control navigation order places `Cloudflare 优选` directly after `网络设置` and before `代理与规则源`. The page exposes domain targets, interval/Cron scheduling, bounded scan settings, and current selected IP metrics.

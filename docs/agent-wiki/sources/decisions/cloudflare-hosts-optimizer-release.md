# Release scope

The first Cloudflare optimizer release is QNAP-only and IPv4-only. It does not manage WARP, IPv6, QUIC/UDP probing, or arbitrary Cloudflare domains. Only user-declared hostnames are optimized.

export const controlIntegritySelectors = Object.freeze([
  '.dashboard-header-actions',
  '.group-health-summary',
  '.category-summary',
  '.sticky-save.needs-reload',
  '.profile-overlay-controls',
])
